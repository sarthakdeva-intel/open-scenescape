---
# SPDX-FileCopyrightText: (C) 2025 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

name: "[Code Analysis] Bandit Security Scan"
run-name: "[Code Analysis] Bandit Security Scan"

on:
  workflow_dispatch: {}
  pull_request:
    branches:
      - main
      - release-*
    types:
      - opened
      - synchronize
      - reopened
  push:
    branches:
      - main
      - release-*

  # Trigger workflow when enqueued to a merge group
  # (must be under 'on')
  merge_group:

# Only run at most 1 workflow concurrently per PR or per branch to keep costs down
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read # needed for actions/checkout

jobs:
  bandit:
    name: "Run Bandit Security Scan"
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Run Bandit scan
        if: github.event_name == 'pull_request'
        uses: open-edge-platform/orch-ci/.github/actions/security/bandit@042e5fabb538da85fcb76a3390a1b55c1d4b3500
        with:
          scan-scope: "changed"
          severity-level: "HIGH"
          output-format: "txt"
          config_file: ".github/resources/bandit.config"
          fail-on-findings: "true"
      - name: Run Bandit scan
        if: github.event_name == 'workflow_dispatch' || github.event_name == 'push'
        uses: open-edge-platform/orch-ci/.github/actions/security/bandit@042e5fabb538da85fcb76a3390a1b55c1d4b3500
        with:
          scan-scope: "all"
          severity-level: "HIGH"
          output-format: "txt"
          config_file: ".github/resources/bandit.config"
          fail-on-findings: "true"
