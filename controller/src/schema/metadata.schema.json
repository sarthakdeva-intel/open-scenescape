{
  "meta:license": [
    "SPDX-FileCopyrightText: (C) 2021 - 2026 Intel Corporation",
    "SPDX-License-Identifier: Apache-2.0"
  ],
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "type": "object",
  "definitions": {
    "id": {
      "title": "Object ID",
      "type": "string",
      "description": "Unique ID of the object in the scene.",
      "examples": ["sensor1", "AB:CD:EF:12:34:56.0"]
    },
    "timestamp": {
      "title": "Timestamp",
      "type": "string",
      "format": "date-time",
      "description": "Timestamp in ISO 8601 UTC format at time of data acquisition.",
      "examples": [
        "2021-01-25T23:26:12Z",
        "2021-01-25T23:26:12.424Z",
        "2021-01-25T23:26:12.424659Z"
      ]
    },
    "rate": {
      "title": "Sensor rate",
      "type": "number",
      "description": "The rate at which the sensor is producing data.",
      "minimum": 0.0
    },
    "bounding_box": {
      "type": "object",
      "title": "Bounding Box",
      "description": "Bounding box for signaling the normalized camera coordinates of a detected object. These coordinates should be in normalized camera space.",
      "properties": {
        "x": {
          "type": "number",
          "description": "x coordinate of the top-left corner of bounding box."
        },
        "y": {
          "type": "number",
          "description": "y coordinate of the top-left corner of bounding box."
        },
        "width": {
          "type": "number",
          "description": "Horizontal width of the bounding box.",
          "minimum": 0
        },
        "height": {
          "type": "number",
          "description": "Vertical height of the bounding box.",
          "minimum": 0
        }
      },
      "required": ["x", "y", "width", "height"]
    },
    "bounding_box_px": {
      "type": "object",
      "title": "Bounding Box",
      "description": "Bounding box for signaling the normalized camera coordinates of a detected object. These coordinates should be in camera pixel space.",
      "properties": {
        "x": {
          "type": "number",
          "description": "x coordinate of the top-left corner of bounding box."
        },
        "y": {
          "type": "number",
          "description": "y coordinate of the top-left corner of bounding box."
        },
        "z": {
          "type": "number",
          "description": "z coordinate of the top-left corner of bounding box."
        },
        "width": {
          "type": "number",
          "description": "Horizontal width of the bounding box.",
          "minimum": 0
        },
        "height": {
          "type": "number",
          "description": "Vertical height of the bounding box.",
          "minimum": 0
        },
        "depth": {
          "type": "number",
          "description": "Depth of the bounding box.",
          "minimum": 0
        }
      },
      "required": ["x", "y", "width", "height"]
    },
    "semantic_metadata_attribute": {
      "type": "object",
      "title": "Semantic Metadata Attribute",
      "description": "A semantic attribute detected by an analytics model with label, confidence, and model source.",
      "additionalProperties": true,
      "properties": {
        "label": {
          "title": "Label",
          "description": "The detected value or label for this attribute (e.g., 'Female' for gender, 'blue' for color, true/false for boolean attributes)."
        },
        "confidence": {
          "title": "Confidence",
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Confidence score of the detected attribute (0.0 to 1.0)."
        },
        "model_name": {
          "title": "Model Name",
          "type": "string",
          "description": "Name or identifier of the model that generated this attribute (e.g., 'age-gender-recognition-retail-0013')."
        }
      },
      "required": ["label", "model_name"]
    },
    "semantic_metadata": {
      "type": "object",
      "title": "Semantic Metadata",
      "description": "Semantic attributes describing what an object is. Contains the extensible set of all semantic properties detected by analytics models. Each attribute follows the structure: {label, confidence, model_name}.",
      "additionalProperties": {
        "oneOf": [
          {
            "$ref": "#/definitions/semantic_metadata_attribute"
          },
          {
            "type": "object",
            "properties": {
              "embedding_vector": {
                "type": "string"
              },
              "embedding_dimensions": {
                "type": "integer",
                "minimum": 32
              },
              "model_name": {
                "type": "string"
              }
            },
            "required": ["embedding_vector", "model_name"],
            "description": "Special case for reid embedding vectors"
          }
        ]
      },
      "meta:extensible": true
    },
    "keypoint": {
      "type": "object",
      "title": "Pose Keypoint",
      "description": "Single pose keypoint with normalized coordinates.",
      "properties": {
        "name": {
          "type": "string",
          "description": "Keypoint name (for example: nose, eye_l, shoulder_r)."
        },
        "x": {
          "type": "number",
          "description": "Normalized x coordinate in image space (0 to 1)."
        },
        "y": {
          "type": "number",
          "description": "Normalized y coordinate in image space (0 to 1)."
        }
      },
      "required": ["name", "x", "y"]
    },
    "keypoints": {
      "type": "array",
      "title": "Pose Keypoints",
      "description": "Pose keypoints when a pose estimation model is used. Coordinates are normalized to frame dimensions.",
      "items": {
        "$ref": "#/definitions/keypoint"
      }
    },
    "keypoint_connections": {
      "type": "array",
      "title": "Keypoint Connections",
      "description": "Flat list of keypoint-name pairs defining connections (for example: [\"nose\", \"eye_l\", \"nose\", \"eye_r\"]). Length should be 2 x number_of_connections.",
      "items": {
        "type": "string"
      }
    },
    "detection": {
      "type": "object",
      "title": "Object Detection",
      "description": "Metadata of an object detected in an image frame.",
      "additionalProperties": true,
      "meta:extensible": true,
      "properties": {
        "bounding_box": {
          "$ref": "#/definitions/bounding_box"
        },
        "translation": {
          "title": "Translation",
          "type": "array",
          "description": "Translation of the object from coordinate space origin (x, y, z)",
          "items": {
            "type": "number"
          },
          "minItems": 3,
          "maxItems": 3
        },
        "rotation": {
          "title": "Rotation",
          "type": "array",
          "description": "Rotation of the object in quaternion",
          "items": {
            "type": "number",
            "exclusiveMinimum": -1,
            "exclusiveMaximum": 1
          },
          "minItems": 4,
          "maxItems": 4
        },
        "size": {
          "title": "Size",
          "type": "array",
          "description": "Size of the object (x, y, z)",
          "items": {
            "type": "number",
            "exclusiveMinimum": 0
          },
          "minItems": 3,
          "maxItems": 3
        },
        "category": {
          "title": "Category",
          "type": "string",
          "description": "Category or class of object being detected.",
          "examples": ["person", "vehicle", "object"]
        },
        "confidence": {
          "title": "Confidence",
          "type": "number",
          "description": "Inferencing confidence result for this detection.",
          "examples": [0.899463546, 0.93243],
          "exclusiveMinimum": 0.0
        },
        "id": {
          "title": "Detection ID",
          "type": "integer",
          "description": "Detection ID. Not expected to track the same detection from one frame to the next.",
          "minimum": 0
        },
        "metadata": {
          "$ref": "#/definitions/semantic_metadata",
          "description": "Semantic metadata describing what an object is (age, gender, clothing, embedding vectors, etc)."
        },
        "distance": {
          "title": "Distance to Object",
          "type": "number",
          "description": "Distance from the camera/sensor to the detection in meters, for example when using RealSense or Lidar.",
          "examples": [1.2, 3.56]
        },
        "keypoints": {
          "$ref": "#/definitions/keypoints"
        },
        "keypoint_connections": {
          "$ref": "#/definitions/keypoint_connections"
        }
      },
      "required": ["category"],
      "oneOf": [
        { "required": ["translation", "size"] },
        { "required": ["lat_long_alt", "size"] },
        {
          "anyOf": [
            { "required": ["bounding_box"] },
            { "required": ["bounding_box_px"] }
          ]
        }
      ]
    },
    "mac": {
      "title": "System MAC Address",
      "type": "string",
      "description": "MAC address of system that acquired the data.",
      "examples": ["AB:CD:EF:12:34:56", "ab-cd-ef-12-34-56"]
    },
    "external_detection": {
      "type": "object",
      "title": "External Source Object Observation",
      "description": "An object observed by an external source (physical agent or child scene), expressed in the source's local coordinate frame. Unlike camera detections, size is optional: a source that cannot estimate an object's extent may report a point observation.",
      "additionalProperties": true,
      "meta:extensible": true,
      "properties": {
        "translation": {
          "title": "Translation",
          "type": "array",
          "description": "Position of the object relative to the source's local origin (x, y, z).",
          "items": {
            "type": "number"
          },
          "minItems": 3,
          "maxItems": 3
        },
        "rotation": {
          "title": "Rotation",
          "type": "array",
          "description": "Rotation of the object in quaternion (x, y, z, w).",
          "items": {
            "type": "number",
            "minimum": -1,
            "maximum": 1
          },
          "minItems": 4,
          "maxItems": 4
        },
        "size": {
          "title": "Size",
          "type": "array",
          "description": "Size of the object (x, y, z). Optional; omit for a point observation with no known extent.",
          "items": {
            "type": "number",
            "exclusiveMinimum": 0
          },
          "minItems": 3,
          "maxItems": 3
        },
        "category": {
          "title": "Category",
          "type": "string",
          "description": "Category or class of the observed object.",
          "examples": ["person", "vehicle", "object"]
        },
        "confidence": {
          "title": "Confidence",
          "type": "number",
          "description": "Source-reported confidence for this observation.",
          "examples": [0.899463546, 0.93243],
          "exclusiveMinimum": 0.0
        },
        "id": {
          "title": "Source-Local Object ID",
          "type": "string",
          "description": "Identifier the source uses to correlate this observation across messages. Not a Scenescape global ID; the controller does not map or look up this value, it is used only as the observation's local reference."
        },
        "metadata": {
          "$ref": "#/definitions/semantic_metadata",
          "description": "Semantic metadata describing what an object is (age, gender, clothing, embedding vectors, etc)."
        }
      },
      "required": ["category", "translation", "id"]
    },
    "external_pose": {
      "type": "object",
      "title": "External Source Pose",
      "description": "Pose of an external source's local origin, used to transform its object observations into the target scene's coordinate frame.",
      "additionalProperties": false,
      "properties": {
        "reference_frame": {
          "title": "Reference Frame",
          "type": "string",
          "description": "Frame the pose is expressed in. 'wgs84' is a global position, only resolvable when the target scene has valid geospatial calibration. 'scene' is a pose already expressed in the target scene's local coordinates and is only accepted from authorized positioning-service publishers.",
          "enum": ["wgs84", "scene"]
        },
        "lat_long_alt": {
          "title": "Latitude, Longitude, Altitude",
          "type": "array",
          "description": "Global position of the source's local origin (latitude, longitude, altitude in meters). Required when reference_frame is 'wgs84'.",
          "items": {
            "type": "number"
          },
          "minItems": 3,
          "maxItems": 3
        },
        "translation": {
          "title": "Translation",
          "type": "array",
          "description": "Position of the source's local origin in scene-local coordinates (x, y, z). Required when reference_frame is 'scene'.",
          "items": {
            "type": "number"
          },
          "minItems": 3,
          "maxItems": 3
        },
        "rotation": {
          "title": "Rotation",
          "type": "array",
          "description": "Orientation of the source's local origin, as a quaternion (x, y, z, w).",
          "items": {
            "type": "number",
            "minimum": -1,
            "maximum": 1
          },
          "minItems": 4,
          "maxItems": 4
        },
        "position_accuracy_m": {
          "title": "Position Accuracy",
          "type": "number",
          "description": "Estimated accuracy of the reported position in meters, if known.",
          "exclusiveMinimum": 0.0
        },
        "provider": {
          "title": "Pose Provider",
          "type": "string",
          "description": "Informational label for what produced this pose (e.g. 'agent', 'positioning_service'). Not used for authorization.",
          "examples": ["agent", "positioning_service"]
        }
      },
      "required": ["reference_frame"],
      "if": {
        "properties": { "reference_frame": { "const": "wgs84" } }
      },
      "then": {
        "required": ["lat_long_alt"]
      },
      "else": {
        "required": ["translation"]
      }
    },
    "external_source": {
      "title": "External Source Metadata",
      "description": "Unified contract for object observations published by external sources (physical agents such as drones or vehicles, and the Scenescape positioning service) under scenescape/external/{publisher_id}/{thing_type}, where publisher_id matches source_id. Scene membership is via consumer-side binding.",
      "type": "object",
      "additionalProperties": true,
      "meta:extensible": true,
      "properties": {
        "timestamp": {
          "$ref": "#/definitions/timestamp"
        },
        "source_id": {
          "title": "Source ID",
          "type": "string",
          "description": "Identifier of the publishing source; must match the topic publisher_id segment. Combined with the bound scene uid to key the source's pose cache."
        },
        "pose": {
          "$ref": "#/definitions/external_pose",
          "description": "Pose of the source's local origin, for transforming 'objects' into the target scene. May be omitted to reuse the most recently cached, non-expired pose for this source_id."
        },
        "objects": {
          "title": "Observed Objects",
          "type": "array",
          "description": "Objects observed by this source, in its local coordinate frame. May be empty when a message only updates the source's pose.",
          "items": {
            "$ref": "#/definitions/external_detection"
          }
        }
      },
      "required": ["timestamp", "source_id", "objects"]
    },
    "singleton": {
      "title": "Sensor Metadata",
      "description": "Many types of sensors exist for generating data from the physical world. This schema defines the composition of metadata for publishing sensor data into Scenescape.",
      "type": "object",
      "properties": {
        "timestamp": {
          "$ref": "#/definitions/timestamp"
        },
        "id": {
          "$ref": "#/definitions/id"
        },
        "value": {
          "title": "Sensor reading",
          "description": "Value of the sensor reading."
        },
        "subtype": {
          "title": "Sensor Subtype",
          "type": "string",
          "examples": ["humidity", "temperature"]
        },
        "rate": {
          "$ref": "#/definitions/rate"
        }
      },
      "required": ["timestamp", "id", "value"]
    },
    "detector": {
      "title": "Detector Metadata",
      "description": "Many types of detectors exist for generating bounding boxes around objects from image-like sources. This schema defines the composition of metadata for publishing object detection results into Scenescape.",
      "type": "object",
      "properties": {
        "timestamp": {
          "$ref": "#/definitions/timestamp"
        },
        "id": {
          "$ref": "#/definitions/id"
        },
        "objects": {
          "title": "Detected Objects by Category",
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "$ref": "#/definitions/detection"
            }
          }
        },
        "sub_detections": {
          "title": "Sub-Detections",
          "type": "array",
          "description": "Array of sub-detections associated with the detector output.",
          "items": {
            "type": "string"
          },
          "examples": [["license_plate"], ["person", "vest", "apriltag"]]
        }
      },
      "required": ["timestamp", "id", "objects"]
    }
  },
  "properties": {
    "singleton": {
      "$ref": "#/definitions/singleton"
    },
    "detector": {
      "$ref": "#/definitions/detector"
    },
    "external_source": {
      "$ref": "#/definitions/external_source"
    }
  },
  "additionalProperties": true,
  "meta:extensible": true
}
