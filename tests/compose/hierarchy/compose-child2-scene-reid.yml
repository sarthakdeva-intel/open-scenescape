# SPDX-FileCopyrightText: (C) 2026 Intel Corporation
# SPDX-License-Identifier: Apache-2.0

networks:
  scenescape-test:

services:
  child2-scene:
    image: intel/scenescape-controller:${VERSION:-latest}
    init: true
    networks:
      scenescape-test:
    depends_on:
      child2-web:
        condition: service_healthy
      child2-broker:
        condition: service_started
      parent-ntpserv:
        condition: service_started
    environment:
      REID_HOSTNAME: ${CHILD2_REID_HOSTNAME:-reid.scenescape.intel.com}
      REID_PORT: "55555"
      REID_DATABASE: VDMS
      VISIBILITY_TOPIC: regulated
    command: >
      --restauth /run/secrets/controller.auth
      --resturl https://child2-web.scenescape.intel.com/api/v1
      --brokerauth /run/secrets/controller.auth
      --broker child2-broker.scenescape.intel.com
      --ntp parent-ntpserv
      --visibility_topic regulated
      --rewriteBadTime
      --maxlag 10
    volumes:
      - ./:/workspace
      - ./${DBROOT}/child2/media:/workspace/media
      - ./controller/config/tracker-config.json:/home/scenescape/Scenescape/tracker-config.json
      - ${REID_CONFIG_FILE:-./controller/config/reid-config.json}:/home/scenescape/Scenescape/reid-config.json
    secrets:
      - source: root-cert
        target: certs/scenescape-ca.pem
      - source: reid-client-key
        target: certs/scenescape-reid.key
      - source: reid-client-cert
        target: certs/scenescape-reid.crt
      - django
      - controller.auth
    restart: on-failure
